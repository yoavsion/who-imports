pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint:fix
    format:
      run: npm run format
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    test:
      run: npm test
